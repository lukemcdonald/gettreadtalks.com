const boxed = 'bg-white shadow-sm rounded-md space-y-6 bg-white p-6'

const error = 'text-status-error pb-6 mb-6 text-sm border-b border-gray-100'

const formRow = 'block'

const input =
	'appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 mb-5 shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm focus:z-10'

const label = 'inline-block font-medium mb-1'

export default {
	boxed,
	error,
	formRow,
	input,
	label,
}
